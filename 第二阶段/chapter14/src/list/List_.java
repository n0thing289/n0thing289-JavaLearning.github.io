package list;

import java.util.ArrayList;
import java.util.List;

public class List_ {
    public static void main(String[] args) {
        // List集合类中元素有序（添加顺序和去除顺序一致），且可重复
        List list = new ArrayList();
        list.add("jack");
        list.add("jack");

        //2. 每个元素都有其对对应的顺序索引，支持索引
        //索引是从零开始的



    }
}
